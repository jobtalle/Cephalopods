Mutator.prototype.mutateNeuron = function(dna) {
    // const decayMin = 0;
    // const decayMax = 2;
    //
    // dna.decay += (-1 + 2 * Math.random()) * .05;
    //
    // if (dna.decay > decayMax)
    //     dna.decay = decayMax;
    // else if (dna.decay < decayMin)
    //     dna.decay = decayMin;
};